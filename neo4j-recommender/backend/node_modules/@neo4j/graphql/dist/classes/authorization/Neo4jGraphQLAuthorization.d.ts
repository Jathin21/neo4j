import type { Neo4jAuthorizationSettings } from "../../types";
import type { JWTPayload } from "jose";
import type { Neo4jGraphQLContext } from "../../types/neo4j-graphql-context";
export declare class Neo4jGraphQLAuthorization {
    private authorization;
    private resolvedKey;
    private unresolvedKey;
    constructor(authorization: Neo4jAuthorizationSettings);
    get globalAuthentication(): boolean;
    decode(context: Neo4jGraphQLContext): Promise<JWTPayload | undefined>;
    decodeBearerTokenWithVerify(bearerToken: string | undefined): Promise<JWTPayload | undefined>;
    private serializeKey;
    private resolveKey;
    private verify;
    private verifyBearerToken;
}
//# sourceMappingURL=Neo4jGraphQLAuthorization.d.ts.map